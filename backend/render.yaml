services:
  - type: web
    name: match-alpha-backend
    env: docker
    plan: free
    healthCheckPath: /api/v1/health
    envVars:
      - key: APP_ENV
        value: production
      - key: API_PREFIX
        value: /api/v1
      - key: DEFAULT_SEASON_SLUG
        value: wc2026

